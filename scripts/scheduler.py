#from apscheduler.schedulers.blocking import BlockingScheduler
#import main  # <-- import the actual function


#scheduler = BlockingScheduler()

# Hourly task
# scheduler.add_job(lambda: main(freq="hourly"), 'interval', hours=1)
#scheduler.add_job(lambda: main(freq="minute"), 'interval', minutes=1)

# Daily task
#scheduler.add_job(lambda: main(freq="daily"), 'cron', hour=0)

#pythsttescheduler.start()
